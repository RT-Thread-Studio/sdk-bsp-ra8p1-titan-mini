# 视觉示例

本目录包含 124 个视觉示例：相机、拍照录像、光流、绘图、滤波、颜色跟踪、帧差、条码、特征检测和 AprilTag。

运行前将当前固件配套的 `openmv-vision.bin` 放在存储介质根目录，等串口报告验证成功。连接 OpenMV IDE，单独打开一个 `.py` 运行。使用摄像头的示例需要本板 OV5640 MIPI；在固定光照下运行，颜色阈值、镜头校正系数与检测阈值需按现场调整。

| 分类 | 内容 |
| --- | --- |
| [入门](00_入门/README.md) | 相机预览和帧率 |
| [拍照](01_相机/00_拍照/README.md) | 普通、浮雕、运动/人脸触发、定时拍照 |
| [录像](01_相机/01_录像/README.md) | GIF、MJPEG、磁盘与内存图像流 |
| [光流](01_相机/02_光流/README.md) | 整帧/分块、绝对/差分平移与旋转缩放 |
| [传感器控制](01_相机/03_传感器控制/README.md) | 曝光、增益、白平衡、镜像、翻转、参数恢复 |
| [绘图](02_图像处理/00_绘图/README.md) | 几何绘制、调色板、缩放、透明度混合 |
| [滤波与变换](02_图像处理/01_滤波与变换/README.md) | 卷积、阈值、形态学、直方图、畸变、透视、极坐标、ulab |
| [色块与巡线](02_图像处理/02_色块与巡线/README.md) | RGB/灰度色块、颜色编码、自动阈值、统计 |
| [帧差与相似度](02_图像处理/03_帧差与相似度/README.md) | 固定/动态背景、SSIM、磁盘/内存参考帧 |
| [条码](03_条码/README.md) | 一维码、二维码、DataMatrix |
| [特征检测](04_特征检测/README.md) | 线、圆、矩形、关键点、HOG、LBP、模板匹配、区域搜索 |
| [AprilTag](05_AprilTag/README.md) | 标签识别、位姿、高分辨率、ROI 小标签 |

相机采集端只配置 `QQVGA`、`QVGA`、`VGA`，光流用 `QVGA` 配合 `window()` 获得 64×64 或 128×128。图像 `scale(hint=image.TRANSPOSE)` 是受支持的软件变换，不调用未实现的相机转置采集。自动对焦、动态 readout、事件相机、热成像、ToF、全局快门专用例未收录，具体原因记录在总清单。

人脸拍照/录像及 `lbp.py` 保留官方 `/rom/haarcascade_frontalface.cascade`。本板 `/rom` 是存储根目录的只读别名：把发布 `media/haarcascade_frontalface.cascade` 复制到 U 盘根目录，不用建立 `rom` 文件夹。模型为完整 25 级，许可保留在 `assets/LICENSE.frontalface.txt`。这些脚本只作 LED 编号或采集尺寸等必要适配；官方的完成提示、退出方式和 LBP 输出格式保留。`lbp.py` 中官方打印的 `Match %` 实际是 LBP 描述符距离（越小越相近），并非匹配百分比。

写文件的示例需要可写 FAT（默认 SD，也支持所选 SPI Flash）。运行时请让电脑停止访问该盘；运行结束后再读取文件。示例会创建或覆盖注释中列出的 `example.*`、`stream.bin`、`desc.*`、`temp/` 和 `timelapse/` 文件。运动/人脸触发脚本会重复保存，请留足空间并在观察到结果后停止。原始流限制为 24 帧；内存流限制为 20 帧。定时拍照例持续供电计时，不使用此端口尚未实现的 RTC 唤醒和深睡眠。

AprilTag 与二维码是不同编码，AprilTag 示例默认识别 TAG36H11。三维位姿例保留六自由度打印；示例内参来自上游演示，必须使用实际 OV5640 镜头标定参数才能作定量测量。没有检测结果时，先确认标签家族、目标清晰度与照明。

检查依据为当前固件视觉功能报告、MIPI 驱动和 Python 接口实现，全部脚本通过本工程 `mpy-cross` 编译。编译和接口检查不能替代摄像头、光照、目标与存储组合的实板运行验证。
