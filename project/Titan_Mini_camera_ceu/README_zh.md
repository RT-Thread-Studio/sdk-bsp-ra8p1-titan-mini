# CEU 摄像头使用说明

**中文**|[**English**](README.md)

## 简介

本示例展示了如何在 **Titan Board Mini** 上使用 **CEU（Camera Engine Unit）** 接口连接 **OV5640 摄像头**，并通过 **RT-Thread LCD 框架** 将捕获的图像显示到 **RGB565 LCD 屏幕**。

主要功能包括：

- 初始化 CEU 摄像头接口，捕获实时视频流
- 配置 OV5640 摄像头参数（分辨率、帧率、输出格式）
- 使用 RT-Thread LCD 驱动显示摄像头捕获的图像
- 支持图像格式转换（YUV422 → RGB565）

## RA8 系列 CEU（Camera Engine Unit）特性

RA8 系列 MCU 内置 **CEU 硬件模块**，用于高效捕获摄像头图像数据，并支持多种图像格式和分辨率，直接将数据传输到内存或显示接口。

### 1. CEU 硬件接口特性

1. **接口类型**
   - **DVP（Digital Video Port）** 接口，用于连接 CMOS 摄像头
   - 支持 8/10/12 位数据总线
   - 同步信号：
     - **PCLK**：像素时钟
     - **HSYNC**：行同步信号
     - **VSYNC**：帧同步信号
2. **输入分辨率与帧率**
   - 支持 VGA、QVGA、SXGA、UXGA 等常用分辨率
   - 帧率支持 1–60 fps，可配置以适应不同应用
3. **摄像头兼容性**
   - 与 OV5640、OV7670 等常用 CMOS 摄像头兼容
   - 支持自动初始化和寄存器配置

### 2. 图像格式与处理能力

1. **图像格式支持**
   - **YUV422**（常用用于视频传输）
   - **RGB565**（适合 LCD 显示）
   - **RAW10/RAW12**（用于图像处理和算法开发）
2. **图像处理功能**
   - **颜色空间转换**：YUV ↔ RGB
   - **图像裁剪**：可只采集部分 ROI（Region of Interest）
   - **图像缩放**：支持按比例缩小或放大
   - **镜像与翻转**：可水平或垂直镜像
3. **硬件加速**
   - CEU 内置硬件处理单元，可减轻 CPU 负担
   - 提供快速图像格式转换和缩放功能

### 3. DMA 支持与缓冲机制

1. **高速 DMA 传输**
   - 与 MCU 的 DMAC 配合，实现内存高速写入
   - 支持直接将图像写入 Frame Buffer 或 LCD 缓冲区
2. **多缓冲区机制**
   - 支持双缓冲或环形缓冲，保证连续视频采集
   - 减少帧丢失和显示延迟
3. **DMA 配置灵活**
   - 可配置缓冲区起始地址、大小
   - 支持中断触发与回调

### 4. 中断机制

1. **中断类型**
   - **帧完成中断（Frame End）**：每帧采集完成时触发
   - **行中断（Line End，可选）**：每行采集完成时触发
   - **错误中断**：包括缓冲区溢出、同步信号异常
2. **中断特点**
   - 支持 RT-Thread ISR 回调注册
   - 可与 DMA 联动，实现实时处理和显示

### 5. 时序与同步特性

1. **行/帧同步**
   - HSYNC 对齐每行数据
   - VSYNC 对齐每帧图像
2. **像素时钟**
   - CEU 支持外部 PCLK 或内部分频
   - 确保与摄像头输出同步，避免采样错误
3. **数据对齐**
   - 支持字节对齐或像素对齐
   - 可根据图像格式自动调整

### 6. 性能与优化

1. **高吞吐量**
   - DMA + 双缓冲机制实现连续视频采集
   - CPU 占用低，适合实时应用
2. **可靠性**
   - 同步信号异常可触发中断
   - 缓冲区溢出检测
   - 支持帧丢失自动恢复
3. **灵活性**
   - 支持多分辨率和多格式切换
   - 可配置裁剪和缩放区域，提高显示效率

### 7. 应用场景

- 实时视频显示到 LCD
- 视频采集与处理算法测试
- 嵌入式视觉应用，如监控、手势识别、机器人视觉

## OV5640 摄像头驱动

本项目使用 **OV5640** 500万像素 CMOS 摄像头，通过 **I2C 接口**进行控制配置，**DVP 接口**传输图像数据。

### 1. 硬件连接

OV5640 摄像头与 Titan Board 的连接：
- **I2C 控制接口**：配置摄像头寄存器参数
- **DVP 数据接口**：8位并行数据总线
  - D0-D7：图像数据
  - PCLK：像素时钟
  - HSYNC：行同步
  - VSYNC：帧同步
- **控制引脚**：PWDN（电源休眠）、RESET（复位）

### 2. 驱动架构

摄像头驱动采用分层设计：

```
应用层 (hal_entry.c)
    ↓
Sensor 抽象层 (sensor.h/sensor.c)
    ↓
OV5640 驱动层 (ov5640.c/ov5640_regs.h)
    ↓
CEU 硬件接口层 (r_ceu.c)
```

**核心组件**：
- **sensor.h/c**：统一的传感器抽象层接口
- **ov5640.c/ov5640_regs.h**：OV5640 具体驱动实现和寄存器定义
- **sensor_utils.c**：传感器通用工具函数

### 3. 支持的功能

OV5640 驱动支持以下功能：
- **多种分辨率**：从 QQVGA (160×120) 到 UXGA (1600×1200)
- **像素格式**：RGB565、YUV422、JPEG 等
- **图像控制**：
  - 自动/手动增益控制（AGC）
  - 自动/手动曝光控制（AEC）
  - 自动白平衡（AWB）
  - 亮度、对比度、饱和度调节
- **图像特效**：
  - 水平/垂直镜像
  - 色彩条测试模式
  - 特殊数字效果

## 软件架构

### 1. 工程结构

```
Titan_Mini_camera_ceu/
├── applications/         # 应用代码（如使用）
├── src/                  # 源文件
│   └── hal_entry.c      # 主程序入口
├── board/               # 板级配置
├── ra/                  # Renesas FSP 库
│   └── fsp/src/r_ceu/  # CEU 驱动实现
├── ra_gen/              # 生成的配置代码
├── ra_cfg/              # FSP 配置文件
│   └── fsp_cfg/
│       └── r_ceu_cfg.h # CEU 配置
└── libraries/           # 第三方库
    └── Common/ports/ceu_camera/ # CEU 摄像头驱动
        ├── sensor.h/c        # 传感器抽象层
        └── sensors/
            └── ov5640.c/h   # OV5640 驱动
```

### 2. 主程序流程

主程序 (`src/hal_entry.c`) 实现了完整的摄像头采集和显示流程：

```c
void hal_entry(void)
{
    // 1. 初始化传感器
    sensor_init();

    // 2. 复位传感器
    sensor_reset();

    // 3. 设置像素格式为 RGB565
    sensor_set_pixformat(PIXFORMAT_RGB565);

    // 4. 设置分辨率为 VGA (640×480)
    sensor_set_framesize(FRAMESIZE_VGA);

    // 主循环
    while(1)
    {
        // 5. 清空图像缓冲区
        memset(g_image_rgb565_sdram_buffer, 0, buffer_size);

        // 6. 捕获一帧图像
        sensor_snapshot(&sensor, g_image_rgb565_sdram_buffer, 0);

        // 7. 将图像显示到 LCD
        lcd_draw_jpg(0, 0, g_image_rgb565_sdram_buffer, CAM_WIDTH, CAM_HEIGHT);

        // 8. 短暂延时
        rt_thread_mdelay(1);

        // 9. LED 状态指示
        led_status = !led_status;
        rt_pin_write(LED_PIN_1, led_status ? PIN_HIGH : PIN_LOW);
    }
}
```

### 3. 内存管理

- **图像缓冲区**：使用 SDRAM 存储图像数据
  - 大小：`640 × 480 × 2 = 600KB`（RGB565 格式）
  - 放置于 `.bss` 段，8 字节对齐
- **SDRAM 驱动**：`drv_sdram.c` 提供外部 SDRAM 初始化和访问

## 显示输出

本示例使用 **RGB565 LCD 屏幕** 作为显示输出设备，通过 RT-Thread LCD 驱动框架进行控制：

- **显示分辨率**：与摄像头采集分辨率匹配（VGA 640×480）
- **色彩格式**：RGB565（16位色深）
- **显示驱动**：基于 RT-Thread LCD 设备驱动框架
- **图像显示**：将 CEU 捕获并转换后的图像数据实时刷新到 LCD 屏幕
- **显示接口**：使用 `lcd_draw_jpg()` 函数绘制图像

## 运行效果

### 1. 终端输出

复位 Titan Board Mini 后终端会输出如下信息：

![image-20250815150100702](figures/image-20250815150100702.png)

### 2. 显示效果

LCD 屏幕将实时显示 OV5640 摄像头捕获的图像：
- **分辨率**：640×480
- **帧率**：约 30 FPS（受 LCD 刷新速度影响）
- **色彩**：RGB565，16 位色深，显示效果良好
- **LED 指示**：板载 LED 会随帧采集闪烁

## 调试与优化

### 1. 常见问题

- **图像花屏**：检查 DVP 接线、时序配置
- **帧率低**：优化 LCD 驱动、使用双缓冲
- **颜色异常**：检查像素格式配置、字节序


## 扩展应用

基于本示例，可以扩展以下应用：
- **图像处理**：添加图像算法（边缘检测、颜色识别）
- **视频录制**：将图像保存到 SD 卡
- **网络传输**：通过以太网/WiFi 传输视频流
- **人脸识别**：集成 AI 算法实现人脸检测
- **运动检测**：实现帧差法运动目标检测